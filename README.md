# ProcessCore Dataset — legacy data lineage

> **Status: LEGACY DATA / PUBLIC**
>
> This repository preserves an older ProcessCore data/export pipeline. It is not the current ProcessCore authority and must not be used as the source of current project state.

The historical implementation remains under the nested project directory for provenance. New ProcessCore, QCore, AgriCore, TDK, Golden, or other current project work should use dedicated current repositories.

No current credentials, runtime databases, project ledgers, or authoritative evidence heads should be added here.
